<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <a class="navbar-brand" href="#">Chirp</a>
    </div>
    <% if current_user %>
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li id="friends"><a href="#">Friends</a></li>
        <li id="followers" ><a href="#">Followers</a></li>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "#{current_user.username}", dashboard_show_path %></li>
        <li><%= link_to "Logout", "/logout", method: :delete, class: "btn btn-lg btn-default btn-home" %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
